{
  "name": "unipass-wallet-dkim-base",
  "version": "0.0.12",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "test": "jest --testTimeout 30000",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "ethers": "^5.6.9"
  },
  "devDependencies": {
    "@jest/types": "~28.1.0",
    "@types/jest": "~27.5.0",
    "@types/node": "^18.0.5",
    "jest": "~28.0.0",
    "rimraf": "~3.0.2",
    "ts-jest": "~28.0.0",
    "typescript": "~4.7.0"
  }
}
