{
  "name": "unipass-wallet-abi",
  "version": "0.0.14",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run prebuild && pnpm run compile",
    "prebuild": "ts-node ./build.ts",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^18.0.5",
    "rimraf": "~3.0.2",
    "typescript": "~4.7.0"
  }
}
